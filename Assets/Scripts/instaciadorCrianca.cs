using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class instaciadorCrianca : MonoBehaviour
{
   /* public Vector3 posicaoParaInstanciar;
    public float rotacaoEmX, rotacaoEmY, rotacaoEmZ;
    public GameObject[] criancas;
    // Start is called before the first frame update
    void Start()
    {
        Instantiate(criancas[DATACRIANCA.CriancaAInstanciar], posicaoParaInstanciar, Quaternion.Euler(rotacaoEmX, rotacaoEmY, rotacaoEmZ));
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
