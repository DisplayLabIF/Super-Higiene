using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class playersFaseBucal : MonoBehaviour
{
   /* public GameObject[] poses;
    public Texture[] posicaoCrianca;
    public static int posicaoSelecionada;
    public static int posicaoAInstanciar;
    // Start is called before the first frame update
    void Start()
    {
        if (EscolhaCrianca.SelecaoAtual == 0)
        {
            posicaoAInstanciar = 0;
        }
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void Awake()
    {
        poses = GameObject.FindGameObjectsWithTag("poses");
        if (poses.Length >= 5)
        {
            Destroy(poses[0]);
        }
        DontDestroyOnLoad(transform.gameObject);
    }*/
}
